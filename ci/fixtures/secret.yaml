# Test credentials for CI only (matches dev/minio.yaml root secrets).
apiVersion: v1
kind: Secret
metadata:
  name: minio-backups-creds
  namespace: aluminium
type: Opaque
stringData:
  access-key: minio-backups
  secret-key: minio-backups-secret

---
apiVersion: v1
kind: Secret
metadata:
  name: minio-media-creds
  namespace: copper
type: Opaque
stringData:
  access-key: minio-media
  secret-key: minio-media-secret
