apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: s3viewers.s3viewer.dev
spec:
  group: s3viewer.dev
  names:
    categories: []
    kind: S3Viewer
    plural: s3viewers
    shortNames: []
    singular: s3viewer
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .status.message
      name: Message
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for S3ViewerSpec via `CustomResource`
        properties:
          spec:
            properties:
              configNamespaces:
                description: Namespaces to scan for S3ViewerConfig resources. Each config's accounts become buckets in the UI. Defaults to the S3Viewer's own namespace when unset or empty.
                items:
                  type: string
                nullable: true
                type: array
              image:
                description: Container image for the s3-viewer app. Defaults to S3_VIEWER_DEFAULT_IMAGE or ghcr.io/tchoupinax/s3-viewer:latest.
                nullable: true
                type: string
              ingress:
                nullable: true
                properties:
                  className:
                    nullable: true
                    type: string
                  host:
                    type: string
                  tlsSecretName:
                    nullable: true
                    type: string
                required:
                - host
                type: object
              replicas:
                default: 1
                format: int32
                type: integer
              service:
                nullable: true
                properties:
                  port:
                    default: 3000
                    format: int32
                    type: integer
                  type:
                    default: ClusterIP
                    type: string
                type: object
            type: object
          status:
            nullable: true
            properties:
              lastSyncTime:
                nullable: true
                type: string
              message:
                nullable: true
                type: string
              ready:
                type: boolean
              url:
                nullable: true
                type: string
            required:
            - ready
            type: object
        required:
        - spec
        title: S3Viewer
        type: object
    served: true
    storage: true
    subresources:
      status: {}
